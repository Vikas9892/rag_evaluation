# Copy to .env.local and adjust. .env.local is git-ignored.

# Base URL of the FastAPI backend. The browser calls it directly (ADR 008),
# so this value is inlined into the client bundle and is public by definition.
# Never put a secret behind a NEXT_PUBLIC_ prefix.
NEXT_PUBLIC_API_URL=http://localhost:8000
