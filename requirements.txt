# Core
pymupdf>=1.24.0
langchain-text-splitters>=0.3.0
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
groq>=0.9.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
# Required by POST /documents: FastAPI raises at import time, not at request
# time, when a route declares UploadFile without it — so a missing multipart
# takes the whole app down rather than one endpoint.
python-multipart>=0.0.9

# Evaluation
rouge-score>=0.1.2
nltk>=3.8.1

# AWS
boto3>=1.34.0
mangum>=0.17.0

# Retrieval extensions
rank-bm25>=0.2.2

# Load testing
locust>=2.20.0

# Dev / testing
pytest>=8.2.0
pytest-cov>=5.0.0
ruff>=0.6.0
black>=24.0.0
